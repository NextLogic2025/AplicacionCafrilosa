# ============================================
# Configuración del Servicio de Autenticación
# ============================================

# Ambiente: development | production | test
NODE_ENV=development

# Puerto del servidor
PORT=3001

# Base de datos PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/auth_db

# ============================================
# 🔒 SECRETS DE SEGURIDAD (OBLIGATORIOS)
# ============================================
# IMPORTANTE: Cada ambiente (dev, staging, prod) debe tener valores DIFERENTES
# Generar secrets seguros con: openssl rand -base64 32
# O en Node.js: require('crypto').randomBytes(32).toString('base64')

# Secret para firmar Access Tokens (mínimo 32 caracteres)
JWT_SECRET=CAMBIAR_EN_PRODUCCION_openssl_rand_base64_32

# Secret para firmar Refresh Tokens (mínimo 32 caracteres, DIFERENTE a JWT_SECRET)
JWT_REFRESH_SECRET=CAMBIAR_EN_PRODUCCION_otro_secreto_diferente

# ============================================
# Configuración de Tokens
# ============================================

# Tiempo de vida del Access Token (ejemplos: 15m, 1h, 12h)
ACCESS_TOKEN_TTL=12h

# Tiempo de vida del Refresh Token (ejemplos: 1d, 7d, 30d)
REFRESH_TOKEN_TTL=7d

# Forzar una sola sesión activa por usuario (true/false)
SINGLE_SESSION=false

# ============================================
# CORS
# ============================================
# Orígenes permitidos separados por comas
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# ============================================
# ROTACIÓN DE SECRETS (Documentación)
# ============================================
# Para rotar secrets en producción:
# 1. Generar nuevo secret: openssl rand -base64 32
# 2. Actualizar variable en el entorno de producción
# 3. Reiniciar el servicio (los tokens existentes serán invalidados)
# 4. Los usuarios deberán volver a autenticarse
#
# NOTA: Considerar implementar versionado de secrets para rotación gradual
