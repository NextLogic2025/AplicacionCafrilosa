# Inventario Service

Gestiona stock y movimientos.

Versión de Node: `.nvmrc` contiene `22.14.0`.

Arranque rápido:

```bash
cd Backend/gestion-ventas/services/inventario-service
nvm use
npm install
npm run dev
```
