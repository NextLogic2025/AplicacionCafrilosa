# BFF Service

Backend-for-Frontend (agregaciones, caché por vista, control de permisos).

Versión de Node: `.nvmrc` contiene `22.14.0`.

Arranque rápido:

```bash
cd Backend/gestion-ventas/services/bff-service
nvm use
npm install
npm run dev
```
