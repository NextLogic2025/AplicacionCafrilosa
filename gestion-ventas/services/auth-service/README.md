# Auth Service

Responsabilidad: autenticación (AuthN), emisión de JWT, roles y permisos.

Versión de Node: `.nvmrc` contiene `22.14.0`.

Arranque rápido:

```bash
cd Backend/gestion-ventas/services/auth-service
nvm use
npm install
npm run dev
```
