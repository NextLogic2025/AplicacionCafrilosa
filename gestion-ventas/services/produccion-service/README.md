# Producción Service

Gestiona lotes y trazabilidad.

Versión de Node: `.nvmrc` contiene `22.14.0`.

Arranque rápido:

```bash
cd Backend/gestion-ventas/services/produccion-service
nvm use
npm install
npm run dev
```
