# Productos Service

Catálogo de productos y metadatos.

Versión de Node: `.nvmrc` contiene `22.14.0`.

Arranque rápido:

```bash
cd Backend/gestion-ventas/services/productos-service
nvm use
npm install
npm run dev
```
