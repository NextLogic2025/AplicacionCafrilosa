# Clientes Service

Gestión de clientes y cuentas.

Versión de Node: `.nvmrc` contiene `22.14.0`.

Arranque rápido:

```bash
cd Backend/gestion-ventas/services/clientes-service
nvm use
npm install
npm run dev
```
