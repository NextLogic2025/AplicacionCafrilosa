# Users Service

Gestión de usuarios y perfiles.

Versión de Node: `.nvmrc` contiene `22.14.0`.

Arranque rápido:

```bash
cd Backend/gestion-ventas/services/users-service
nvm use
npm install
npm run dev
```
